const today=new Date(),calendar=document.querySelector('#calendar'),monthTitle=document.querySelector('#month-title');let viewDate=new Date(today.getFullYear(),today.getMonth(),1);const recordDays={3:'positive',5:'positive',8:'positive',9:'negative',10:'positive',12:'positive',14:'positive',17:'positive',20:'negative',22:'positive',25:'positive',28:'positive'};
document.querySelector('#entry-date').textContent=today.toLocaleDateString('ko-KR',{year:'numeric',month:'long',day:'numeric'});
function renderCalendar(){const year=viewDate.getFullYear(),month=viewDate.getMonth(),firstDay=new Date(year,month,1).getDay(),lastDate=new Date(year,month+1,0).getDate(),previousLast=new Date(year,month,0).getDate();monthTitle.textContent=`${year}년 ${month+1}월`;calendar.innerHTML='';for(let offset=firstDay-1;offset>=0;offset--)calendar.insertAdjacentHTML('beforeend',`<button class="day muted">${previousLast-offset}</button>`);for(let day=1;day<=lastDate;day++){const button=document.createElement('button');button.className='day';button.textContent=day;if(recordDays[day])button.classList.add('has-entry',recordDays[day]);if(year===today.getFullYear()&&month===today.getMonth()&&day===today.getDate())button.classList.add('today');button.addEventListener('click',()=>{document.querySelectorAll('.day').forEach(item=>item.classList.remove('selected'));button.classList.add('selected');document.querySelector('#selected-date-label').textContent=`${month+1}월 ${day}일의 기록`;});calendar.appendChild(button);}}
document.querySelector('#prev-month').addEventListener('click',()=>{viewDate=new Date(viewDate.getFullYear(),viewDate.getMonth()-1,1);renderCalendar();});document.querySelector('#next-month').addEventListener('click',()=>{viewDate=new Date(viewDate.getFullYear(),viewDate.getMonth()+1,1);renderCalendar();});document.querySelector('.new-entry').addEventListener('click',()=>{const toast=document.querySelector('.toast');toast.textContent='일기 작성 기능은 백엔드 연결 후 제공될 예정입니다. 🌱';toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2300);});renderCalendar();
