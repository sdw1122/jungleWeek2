const cards=[...document.querySelectorAll('.plant-card')],filters=document.querySelectorAll('.filter'),search=document.querySelector('#search'),bar=document.querySelector('.selection-bar'),selectedName=document.querySelector('#selected-name');let category='all',selected=null;
cards.forEach(card=>card.querySelector('.photo').style.backgroundImage=`url("${card.dataset.image}")`);
function filterCards(){const term=search.value.trim().toLowerCase();let count=0;cards.forEach(card=>{const visible=(category==='all'||card.dataset.category===category)&&card.dataset.name.toLowerCase().includes(term);card.hidden=!visible;if(visible)count++});document.querySelector('.empty').hidden=count>0}
filters.forEach(button=>button.addEventListener('click',()=>{category=button.dataset.filter;filters.forEach(item=>item.classList.toggle('active',item===button));filterCards()}));search.addEventListener('input',filterCards);
cards.forEach(card=>card.querySelector('button').addEventListener('click',()=>{cards.forEach(item=>item.classList.toggle('selected',item===card));selected=card;selectedName.textContent=card.dataset.name;bar.hidden=false}));
document.querySelector('#continue').addEventListener('click',()=>{if(!selected)return;localStorage.setItem('farmdaPlant',JSON.stringify({name:selected.dataset.name,image:selected.dataset.image,category:selected.dataset.category}));document.body.classList.add('page-leave');setTimeout(()=>location.href='dashboard-v2.html',320)});document.querySelector('.close').addEventListener('click',()=>history.back());
